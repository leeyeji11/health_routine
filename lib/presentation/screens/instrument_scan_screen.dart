import 'package:flutter/material.dart';

class InstrumentScanScreen extends StatelessWidget {
  const InstrumentScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('InstrumentScanScreem'),
    );
  }
}
